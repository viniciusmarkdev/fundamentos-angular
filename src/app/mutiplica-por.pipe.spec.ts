import { MutiplicaPorPipe } from './mutiplica-por.pipe';

describe('MutiplicaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new MutiplicaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
